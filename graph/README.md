# Z-ai-governance Graph Data API

> **Audience:** External tools (especially `Z-ai-graph-viewer`) that need to
> display the true state of the Z-ai-governance ID graph.
> **Updated:** On every push to `main` (automated via CI).
> **Format:** JSON (deterministic — same input produces same output).

---

## Why `graph/` (not `docs/_graph/` or `standards/_graph/`)?

Three locations were considered:

| Location                | Why rejected                                                                                                          |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `standards/_graph/`     | Not used — graph data lives exclusively in this `graph/` directory. |
| `docs/_graph/`          | Directory does not exist in this repo.                                                                                |
| **`graph/` (this dir)** | Canonical location for all graph data — both machine-consumable JSON exports and any future visual artifacts.          |

---

## Endpoints

### 1. Full ID graph (for graph visualization)

```http
GET https://raw.githubusercontent.com/stsgs1980/Z-ai-governance/main/graph/id-graph.json
```

**Content-Type:** `application/json`
**CORS:** Supported (raw.githubusercontent.com sends permissive CORS headers)
**Cache-Control:** GitHub serves with long cache lifetime; pin with `?ts=<commit-sha>` if needed

**Response shape:**

```jsonc
{
  "version": "1.1.6", // verify-id-graph.js script version
  "effective_date": "2026-06-21", // when this version of verifier was published
  "exported_at": "2026-07-06T14:30:00Z", // when this JSON was generated
  "summary": {
    "ids_extracted": 53,
    "related_edges": 97,
    "aligned_with_edges": 2,
    "hard_pass": 13,
    "hard_fail": 0,
    "warnings": 0,
  },
  "checks": [/* G01-G15 results, see below */],
  "warnings": [/* W01-W15 soft warnings */],
  "ids": [
    // All extracted IDs
    { "id": "STD-META-001", "prefix": "STD", "domain": "META", "number": "001", "file": "..." },
    {
      "id": "RULE-WORKLOG-002",
      "prefix": "RULE",
      "domain": "WORKLOG",
      "number": "002",
      "file": "...",
    },
  ],
  "related_edges": [{ "from": "STD-META-001", "to": "STD-DOC-002", "file": "..." }],
  "aligned_with_edges": [{ "left": "STD-SKILL-001", "right": "ZAI-DEV-001", "file": "..." }],
}
```

### 2. Summary only (for dashboards / health checks)

```http
GET https://raw.githubusercontent.com/stsgs1980/Z-ai-governance/main/graph/id-graph-summary.json
```

**Response shape:**

```json
{
  "ids_extracted": 53,
  "related_edges": 97,
  "aligned_with_edges": 2,
  "hard_pass": 13,
  "hard_fail": 0,
  "warnings": 0
}
```

### 3. Visual graphs (for static rendering)

> **Note:** Visual graph rendering (SVG/PNG/DOT) is not currently available as a static endpoint. Graph artifacts are generated by CI and uploaded as workflow artifacts (30-day retention).

---

## Directory layout

```text
Z-ai-governance/
└── graph/              # Graph data — JSON exports for API consumers  ← YOU ARE HERE
    ├── id-graph.json
    ├── id-graph-summary.json
    └── README.md
```

---

## How to consume

### TypeScript / React (e.g. Z-ai-graph-viewer)

```typescript
const URL = "https://raw.githubusercontent.com/stsgs1980/Z-ai-governance/main/graph/id-graph.json";

interface GraphData {
  version: string;
  effective_date: string;
  exported_at: string;
  summary: {
    ids_extracted: number;
    related_edges: number;
    aligned_with_edges: number;
    hard_pass: number;
    hard_fail: number;
    warnings: number;
  };
  ids: Array<{
    id: string;
    prefix: string;
    domain: string;
    number: string;
    file: string;
  }>;
  related_edges: Array<{ from: string; to: string; file: string }>;
  aligned_with_edges: Array<{ left: string; right: string; file: string }>;
}

export async function fetchGraph(): Promise<GraphData> {
  const response = await fetch(URL);
  if (!response.ok) throw new Error(`HTTP ${response.status}`);
  return response.json();
}
```

### Recommended: periodic refresh every 5 minutes

```tsx
import { useEffect, useState, useCallback, useRef } from "react";

const REFRESH_INTERVAL_MS = 5 * 60 * 1000;
const STALE_THRESHOLD_MS = 15 * 60 * 1000;

export function useGraphWithAutoRefresh(url: string) {
  const [state, setState] = useState({
    data: null as GraphData | null,
    status: "idle" as "idle" | "loading" | "success" | "error" | "stale",
    lastUpdated: null as Date | null,
    lastError: null as string | null,
    nextRefreshIn: REFRESH_INTERVAL_MS / 1000,
  });
  const intervalRef = useRef<NodeJS.Timeout | null>(null);
  const countdownRef = useRef<NodeJS.Timeout | null>(null);

  const fetchGraph = useCallback(async () => {
    setState((s) => ({ ...s, status: "loading" }));
    try {
      const response = await fetch(url, { cache: "no-cache" });
      if (!response.ok) throw new Error(`HTTP ${response.status}`);
      const data = await response.json();
      setState({
        data,
        status: "success",
        lastUpdated: new Date(),
        lastError: null,
        nextRefreshIn: REFRESH_INTERVAL_MS / 1000,
      });
    } catch (err) {
      setState((s) => ({
        ...s,
        status: "error",
        lastError: err instanceof Error ? err.message : String(err),
      }));
    }
  }, [url]);

  useEffect(() => {
    fetchGraph();
    intervalRef.current = setInterval(fetchGraph, REFRESH_INTERVAL_MS);
    countdownRef.current = setInterval(() => {
      setState((s) => ({ ...s, nextRefreshIn: Math.max(0, s.nextRefreshIn - 1) }));
    }, 1000);
    return () => {
      if (intervalRef.current) clearInterval(intervalRef.current);
      if (countdownRef.current) clearInterval(countdownRef.current);
    };
  }, [fetchGraph]);

  const isStale =
    state.lastUpdated && Date.now() - state.lastUpdated.getTime() > STALE_THRESHOLD_MS;

  return { ...state, isStale, refresh: fetchGraph };
}

// Usage:
const { data, status, lastUpdated, nextRefreshIn, isStale, refresh } = useGraphWithAutoRefresh(
  "https://raw.githubusercontent.com/stsgs1980/Z-ai-governance/main/graph/id-graph.json",
);
```

### Python

```python
import urllib.request, json
url = "https://raw.githubusercontent.com/stsgs1980/Z-ai-governance/main/graph/id-graph.json"
with urllib.request.urlopen(url) as r:
    g = json.loads(r.read())
print(f"IDs: {g['summary']['ids_extracted']}, Edges: {g['summary']['related_edges']}")
```

### curl

```bash
curl -s https://raw.githubusercontent.com/stsgs1980/Z-ai-governance/main/graph/id-graph.json | jq '.summary'
```

---

## Refresh strategy

| Frequency          | Method                                   | When to use                |
| ------------------ | ---------------------------------------- | -------------------------- |
| On every page load | Direct fetch + browser cache             | Live dashboard             |
| **Every 5 min**    | `setInterval` + countdown UI             | **Recommended default**    |
| Every 1 hour       | fetch with `Cache-Control: max-age=3600` | Slightly delayed dashboard |
| On demand          | Manual refresh button                    | Admin tools                |

**Recommended:** periodic refresh every 5 minutes. It balances freshness
(CI takes ~3 min from push to JSON ready) with server load.

### Refresh timeline

```text
T+0:00   Developer pushes to Z-ai-governance main
T+0:30   GitHub Actions CI starts
T+1:00   verify-id-graph.js --json generates new data
T+1:30   CI commits graph/id-graph.json
T+2:00   raw.githubusercontent.com is updated
T+2:30   graph-viewer's setInterval fires → fetches new data
T+3:00   User sees updated graph in browser

Total delay: ~3 minutes from push to visible.
```

---

## Stability contract

The JSON shape is governed by **verify-id-graph.js v1.1.x**. Within v1.1.x:

- `summary.*` fields are stable
- `ids[]`, `related_edges[]`, `aligned_with_edges[]` arrays are stable
- New fields MAY be added (consumers should ignore unknown fields)
- Field renames or removals require a major version bump (v2.0.0)

Pin to a specific version:

```http
GET .../id-graph.json?v=1.1.6
```

When the script version changes, the `version` field in the response will
indicate the new shape. Consumers should check this field.

---

## Source of truth

This JSON is generated by `node standards/scripts/verify-id-graph.js --json`
in the `verify-id-graph.yml` GitHub Actions workflow, then committed back to
the repo on every push to main.

**The data in this file IS the state of Z-ai-governance at the corresponding
commit.** There is no transformation, no caching layer, no manual editing.

If `Z-ai-graph-viewer` shows X, that X is what is actually in Z-ai-governance.

---

## Pinning to a specific commit

For reproducibility, fetch from a specific commit SHA:

```http
GET https://raw.githubusercontent.com/stsgs1980/Z-ai-governance/<commit-sha>/graph/id-graph.json
```

Useful for:

- Archival views
- Reproducible builds
- Comparing versions

---

## Related

- `tests/governance-escalation-report.md` — Defect #12: compliance dashboard
- `standards/scripts/verify-id-graph.js` — Source of the data
- `standards/scripts/graph-deps.sh` — Generates visual graphs
- AGENT_RULES.md §3 — Rule registry (rules enforced)

---

**Last API contract update:** 2026-07-06
**Maintained by:** Z-ai-governance governance scripts (CI-driven)
